dispatch((new App\Jobs\PingJob)->onConnection('redis')->onQueue('default'));
dispatch(function () {
    \Log::info('🧪 Horizon closure job running...');
    sleep(5); // عمداً طولانی تا تو داشبورد دیده بشه
})->onConnection('redis')->onQueue('default');
